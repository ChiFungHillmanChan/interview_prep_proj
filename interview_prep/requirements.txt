# Runtime dependencies for AceInterview.
# Vercel installs this file automatically and bundles it into the Django Function.
# Keep it runtime-only: anything listed here is shipped to production.
#
# Re-audit after changing a pin:
#   python -m pip_audit -r requirements.txt

Django==6.0.7
django-codemirror==1.0.1
django-widget-tweaks==1.5.1
social-auth-app-django==6.0.1

# Database. dj-database-url parses DATABASE_URL; psycopg is the actual driver.
dj-database-url==3.1.2
psycopg[binary]==3.3.4

# Static files. On Vercel these are served from the CDN after the automatic
# collectstatic; WhiteNoise is what serves them under `vercel dev` and locally.
whitenoise==6.12.0

# Configuration
python-decouple==3.8

# Document parsing and resume export (all pure-Python / in-memory).
# pypdf is the maintained successor to PyPDF2, which was abandoned at 3.0.1
# with an unfixed infinite-loop parsing malformed content streams.
pypdf==6.14.2
python-docx==1.2.0
reportlab==4.4.4
Pillow==12.3.0
lxml==6.1.1
beautifulsoup4==4.15.0
chardet==5.2.0
charset-normalizer==3.4.9

# AI
google-genai==2.14.0
requests==2.34.2
